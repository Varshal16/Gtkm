<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Varshal - Real. Not Perfect.</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-primary: #0a0a0a;
            --bg-secondary: #1a1a2e;
            --text-primary: #e5e5e5;
            --text-secondary: #b0b0b0;
            --accent: #ef4444;
            --border: rgba(255, 255, 255, 0.05);
        }

        body.blue-theme {
            --bg-primary: #0d1117;
            --bg-secondary: #161b22;
            --accent: #58a6ff;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            line-height: 1.7;
            overflow-x: hidden;
            transition: background 0.6s ease, color 0.6s ease;
        }

        /* Particle Canvas */
        #particles-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        /* Background Pattern */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(255, 255, 255, 0.01) 2px, rgba(255, 255, 255, 0.01) 4px),
                repeating-linear-gradient(90deg, transparent, transparent 2px, rgba(255, 255, 255, 0.01) 2px, rgba(255, 255, 255, 0.01) 4px);
            z-index: 1;
            pointer-events: none;
        }

        .cursor-follower {
            position: fixed;
            width: 20px;
            height: 20px;
            border: 2px solid var(--accent);
            opacity: 0.5;
            border-radius: 50%;
            pointer-events: none;
            transition: all 0.15s ease;
            z-index: 9999;
            transform: translate(-50%, -50%);
        }

        .cursor-dot {
            position: fixed;
            width: 6px;
            height: 6px;
            background: var(--accent);
            border-radius: 50%;
            pointer-events: none;
            z-index: 10000;
            transform: translate(-50%, -50%);
            transition: all 0.1s ease;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            z-index: 2;
        }

        /* Theme Toggle */
        .theme-toggle {
            position: fixed;
            top: 30px;
            right: 30px;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--border);
            border-radius: 50px;
            padding: 12px 24px;
            cursor: pointer;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        .theme-toggle:hover {
            border-color: var(--accent);
            transform: translateY(-2px);
        }

        /* Quote Box */
        .quote-box {
            position: fixed;
            bottom: 30px;
            left: 30px;
            max-width: 400px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border);
            border-left: 3px solid var(--accent);
            border-radius: 10px;
            padding: 20px;
            backdrop-filter: blur(10px);
            z-index: 1000;
            opacity: 0;
            animation: fadeIn 1s ease forwards 2s;
        }

        .quote-text {
            font-style: italic;
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        @keyframes fadeIn {
            to { opacity: 1; }
        }

        header {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 50% 50%, rgba(239, 68, 68, 0.03), transparent 70%);
            animation: pulse 8s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 0.6; transform: scale(1.1); }
        }

        h1 {
            font-size: clamp(3rem, 10vw, 8rem);
            font-weight: 900;
            letter-spacing: -0.05em;
            position: relative;
            z-index: 1;
            opacity: 0;
            animation: fadeInUp 1s ease forwards 0.3s;
            background: linear-gradient(45deg, var(--accent), #fff, var(--accent));
            background-size: 200% 200%;
            animation: fadeInUp 1s ease forwards 0.3s, gradientShift 3s ease infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .hero-info {
            display: flex;
            gap: 30px;
            margin-top: 20px;
            opacity: 0;
            animation: fadeInUp 1s ease forwards 1s;
        }

        .info-item {
            color: var(--text-secondary);
            font-size: 1rem;
            letter-spacing: 0.1em;
        }

        .info-item span {
            color: var(--accent);
            font-weight: 600;
        }

        .hero-line {
            width: 0;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--accent), transparent);
            margin: 30px 0;
            animation: expandLine 1.5s ease forwards 0.8s;
        }

        @keyframes expandLine {
            to { width: 300px; }
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1, .hero-info, .tagline {
            transform: translateY(30px);
        }

        .tagline {
            font-size: clamp(1rem, 2.5vw, 1.5rem);
            color: #888;
            font-weight: 300;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            opacity: 0;
            animation: fadeInUp 1s ease forwards 1.3s;
            transform: translateY(30px);
        }

        .scroll-indicator {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            opacity: 0;
            animation: fadeInUp 1s ease forwards 1.8s, bounce 2s ease infinite 2.8s;
        }

        .scroll-indicator::before {
            content: '';
            display: block;
            width: 2px;
            height: 40px;
            background: linear-gradient(180deg, transparent, var(--accent));
            margin: 0 auto 10px;
        }

        @keyframes bounce {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(10px); }
        }

        nav {
            position: sticky;
            top: 0;
            background: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
            z-index: 100;
            padding: 20px 0;
        }

        .nav-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        nav button {
            background: transparent;
            color: #888;
            border: 1px solid var(--border);
            padding: 12px 28px;
            font-size: 0.9rem;
            cursor: pointer;
            border-radius: 50px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            font-weight: 500;
            letter-spacing: 0.05em;
            position: relative;
            overflow: hidden;
        }

        nav button::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(239, 68, 68, 0.1);
            transform: translate(-50%, -50%);
            transition: all 0.5s ease;
        }

        nav button:hover::before {
            width: 300px;
            height: 300px;
        }

        nav button:hover {
            color: var(--accent);
            border-color: rgba(239, 68, 68, 0.3);
            transform: translateY(-2px);
        }

        nav button.active {
            background: var(--accent);
            color: #fff;
            border-color: var(--accent);
            box-shadow: 0 0 30px rgba(239, 68, 68, 0.3);
        }

        .read-time {
            display: inline-block;
            background: rgba(255, 255, 255, 0.05);
            padding: 6px 15px;
            border-radius: 20px;
            font-size: 0.85rem;
            color: var(--text-secondary);
            margin-bottom: 20px;
            border: 1px solid var(--border);
        }

        .content-wrapper {
            min-height: 100vh;
            padding: 80px 0;
            opacity: 0;
            transition: opacity 0.6s ease;
        }

        .content-wrapper.visible {
            opacity: 1;
        }

        .section {
            display: none;
        }

        .section.active {
            display: block;
            animation: sectionFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }

        @keyframes sectionFadeIn {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Music Player */
        .music-player {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.02), rgba(255, 255, 255, 0.01));
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 40px;
            margin-bottom: 40px;
            position: relative;
            overflow: hidden;
        }

        .music-player h3 {
            color: var(--accent);
            font-size: 1.8rem;
            margin-bottom: 20px;
        }

        .playlist-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .playlist-item {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border);
            border-radius: 15px;
            padding: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: block;
        }

        .playlist-item:hover {
            border-color: var(--accent);
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(239, 68, 68, 0.2);
        }

        .playlist-name {
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--text-primary);
        }

        .playlist-embed {
            margin-top: 30px;
            border-radius: 15px;
            overflow: hidden;
            display: none;
        }

        .playlist-embed.active {
            display: block;
            animation: fadeIn 0.5s ease;
        }

        .card {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.02), rgba(255, 255, 255, 0.01));
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 50px;
            margin-bottom: 40px;
            position: relative;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(239, 68, 68, 0.05), transparent);
            transition: left 0.7s ease;
        }

        .card:hover::before {
            left: 100%;
        }

        .card:hover {
            border-color: rgba(239, 68, 68, 0.2);
            transform: translateY(-5px);
            box-shadow: 0 20px 60px rgba(239, 68, 68, 0.1);
        }

        h2 {
            font-size: clamp(2rem, 5vw, 3.5rem);
            font-weight: 800;
            color: #fff;
            margin-bottom: 30px;
            letter-spacing: -0.02em;
            position: relative;
            display: inline-block;
            background: linear-gradient(45deg, var(--accent), #fff);
            background-size: 200% 200%;
            animation: gradientShift 3s ease infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--accent), transparent);
            transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .card:hover h2::after {
            width: 100%;
        }

        h3 {
            color: var(--accent);
            font-size: 1.8rem;
            margin: 40px 0 20px 0;
            font-weight: 600;
        }

        p {
            color: var(--text-secondary);
            margin-bottom: 20px;
            font-size: 1.15rem;
            line-height: 1.8;
        }

        ul {
            list-style: none;
            padding-left: 0;
        }

        li {
            padding: 18px 0 18px 40px;
            position: relative;
            color: var(--text-secondary);
            font-size: 1.1rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.03);
            transition: all 0.3s ease;
            opacity: 0;
            animation: listItemFade 0.5s ease forwards;
        }

        li:nth-child(1) { animation-delay: 0.1s; }
        li:nth-child(2) { animation-delay: 0.15s; }
        li:nth-child(3) { animation-delay: 0.2s; }
        li:nth-child(4) { animation-delay: 0.25s; }
        li:nth-child(5) { animation-delay: 0.3s; }
        li:nth-child(6) { animation-delay: 0.35s; }
        li:nth-child(7) { animation-delay: 0.4s; }
        li:nth-child(8) { animation-delay: 0.45s; }
        li:nth-child(9) { animation-delay: 0.5s; }
        li:nth-child(10) { animation-delay: 0.55s; }

        @keyframes listItemFade {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        li {
            transform: translateX(-20px);
        }

        li:hover {
            padding-left: 50px;
            color: var(--text-primary);
            background: rgba(255, 255, 255, 0.02);
        }

        li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 20px;
            height: 2px;
            background: var(--accent);
            transition: width 0.3s ease;
        }

        li:hover::before {
            width: 30px;
        }

        .comparison {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin: 40px 0;
        }

        .before, .after {
            background: rgba(255, 255, 255, 0.02);
            padding: 40px;
            border-radius: 15px;
            border: 1px solid var(--border);
            transition: all 0.5s ease;
            position: relative;
        }

        .before:hover, .after:hover {
            transform: scale(1.02);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
        }

        .before {
            border-top: 3px solid #3b82f6;
        }

        .after {
            border-top: 3px solid var(--accent);
        }

        .before h4, .after h4 {
            text-align: center;
            margin-bottom: 25px;
            font-size: 1.5rem;
            font-weight: 700;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .before h4 { color: #3b82f6; }
        .after h4 { color: var(--accent); }

        .warning {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.05), rgba(239, 68, 68, 0.02));
            border: 1px solid rgba(239, 68, 68, 0.3);
            position: relative;
        }

        .warning::after {
            content: '⚠';
            position: absolute;
            top: 30px;
            right: 30px;
            font-size: 3rem;
            opacity: 0.1;
        }

        footer {
            text-align: center;
            padding: 80px 20px;
            margin-top: 100px;
            border-top: 1px solid var(--border);
            color: #666;
        }

        footer p:first-child {
            font-size: 1.2rem;
            margin-bottom: 20px;
        }

        footer p:last-child {
            color: var(--accent);
            font-size: 1.5rem;
            font-weight: 600;
            letter-spacing: 0.2em;
        }

        .contact-section {
            margin-bottom: 30px;
        }

        .telegram-link {
            display: inline-flex;
            align-items: center;
            padding: 15px 35px;
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(239, 68, 68, 0.05));
            border: 2px solid rgba(239, 68, 68, 0.3);
            border-radius: 50px;
            color: var(--text-primary);
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: 600;
            letter-spacing: 0.05em;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .telegram-link::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(239, 68, 68, 0.2);
            transform: translate(-50%, -50%);
            transition: all 0.6s ease;
        }

        .telegram-link:hover::before {
            width: 400px;
            height: 400px;
        }

        .telegram-link:hover {
            border-color: var(--accent);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 10px 40px rgba(239, 68, 68, 0.3);
        }

        .telegram-link svg {
            transition: transform 0.4s ease;
            position: relative;
            z-index: 1;
        }

        .telegram-link:hover svg {
            transform: rotate(360deg);
        }

        .parallax-text {
            opacity: 0.03;
            font-size: 15rem;
            font-weight: 900;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            pointer-events: none;
            z-index: -1;
            letter-spacing: -0.05em;
        }

        @media (max-width: 768px) {
            .comparison {
                grid-template-columns: 1fr;
            }
            
            .card {
                padding: 30px 25px;
            }
            
            h1 {
                font-size: 3rem;
            }
            
            .parallax-text {
                font-size: 8rem;
            }

            .quote-box {
                left: 20px;
                right: 20px;
                max-width: calc(100% - 40px);
            }

            .theme-toggle {
                top: 20px;
                right: 20px;
            }

            .hero-info {
                flex-direction: column;
                gap: 10px;
            }

            .playlist-grid {
                grid-template-columns: 1fr;
            }
        }

        html {
            scroll-behavior: smooth;
        }

        ::selection {
            background: rgba(239, 68, 68, 0.3);
            color: #fff;
        }

        /* Loading Screen */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #000;
            z-index: 10000;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            transition: opacity 0.8s ease, visibility 0.8s ease;
        }

        .loading-screen.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loading-logo {
            font-size: 5rem;
            font-weight: 900;
            background: linear-gradient(45deg, #ef4444, #fff, #ef4444);
            background-size: 200% 200%;
            animation: gradientShift 2s ease infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 30px;
            letter-spacing: -0.05em;
        }

        .loading-bar-container {
            width: 300px;
            height: 3px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            overflow: hidden;
            position: relative;
        }

        .loading-bar {
            height: 100%;
            background: linear-gradient(90deg, transparent, #ef4444, transparent);
            background-size: 200% 100%;
            animation: loadingSlide 1.5s ease infinite;
            width: 100%;
        }

        @keyframes loadingSlide {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .loading-text {
            margin-top: 20px;
            color: #666;
            font-size: 0.9rem;
            letter-spacing: 0.3em;
            text-transform: uppercase;
        }

        /* Mouse Trail */
        .mouse-trail {
            position: fixed;
            width: 10px;
            height: 10px;
            background: #ef4444;
            border-radius: 50%;
            pointer-events: none;
            z-index: 9998;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        /* Audio Controls */
        .audio-controls {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--border);
            border-radius: 50%;
            width: 60px;
            height: 60px;
            cursor: pointer;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .audio-controls:hover {
            border-color: var(--accent);
            transform: scale(1.1);
            box-shadow: 0 0 20px rgba(239, 68, 68, 0.3);
        }

        .audio-controls.playing {
            background: rgba(239, 68, 68, 0.1);
            border-color: var(--accent);
        }
    </style>
</head>
<body>
    <!-- Loading Screen -->
    <div class="loading-screen" id="loadingScreen">
        <div class="loading-logo">VARSHAL</div>
        <div class="loading-bar-container">
            <div class="loading-bar"></div>
        </div>
        <div class="loading-text">Loading Reality</div>
    </div>

    <!-- Mouse Trail Elements -->
    <div class="mouse-trail" style="transition-delay: 0ms;"></div>
    <div class="mouse-trail" style="transition-delay: 50ms;"></div>
    <div class="mouse-trail" style="transition-delay: 100ms;"></div>
    <div class="mouse-trail" style="transition-delay: 150ms;"></div>
    <div class="mouse-trail" style="transition-delay: 200ms;"></div>

    <!-- Audio Controls -->
    <div class="audio-controls" id="audioToggle" title="Toggle Ambiance">
        🔇
    </div>
    <audio id="ambientAudio" loop>
        <source src="https://cdn.pixabay.com/download/audio/2022/05/13/audio_257112e471.mp3" type="audio/mpeg">
    </audio>

    <canvas id="particles-canvas"></canvas>
    <div class="cursor-follower"></div>
    <div class="cursor-dot"></div>
    <div class="parallax-text">REAL</div>

    <div class="theme-toggle" onclick="toggleTheme()">
        <span id="theme-icon">🌙</span>
        <span id="theme-text">Blue Mode</span>
    </div>

    <div class="quote-box">
        <div class="quote-text" id="quote-text">Loading wisdom...</div>
    </div>

    <header>
        <div class="hero-bg"></div>
        <h1>VARSHAL</h1>
        <div class="hero-line"></div>
        <div class="hero-info">
            <div class="info-item">AGE: <span>16</span></div>
            <div class="info-item">BORN: <span>29 OCT</span></div>
        </div>
        <p class="tagline">Real · Not Perfect · No Fake Energy</p>
        <div class="scroll-indicator"></div>
    </header>

    <nav>
        <div class="nav-container">
            <button onclick="showSection('about')" class="active">About</button>
            <button onclick="showSection('music')">Music</button>
            <button onclick="showSection('evolution')">Evolution</button>
            <button onclick="showSection('truth')">Truth</button>
            <button onclick="showSection('interests')">Interests</button>
            <button onclick="showSection('likes')">Values</button>
            <button onclick="showSection('boundaries')">Boundaries</button>
            <button onclick="showSection('friendship')">Connection</button>
        </div>
    </nav>

    <div class="content-wrapper">
        <div class="container">
            <div id="about" class="section active">
                <span class="read-time">⏱ 2 min read</span>
                <div class="card">
                    <h2>Who I Am</h2>
                    <p>I'm Varshal. I don't talk unless there's something worth saying. I don't connect easily, I don't trust quickly, and I don't like people who pretend to be close but disappear the moment they get bored. I'd rather be alone than surrounded by fake energy.</p>
                    <p>I don't show much of my real personality to the world. Most people only see the quiet version of me. I don't explain myself, I don't justify my silence, and I don't chase anyone. If someone stays, good. If they leave, I won't stop them.</p>
                </div>

                <div class="card">
                    <h2>What Kind of Friend I Am</h2>
                    <ul>
                        <li>I don't talk too much, but I observe everything</li>
                        <li>I won't betray someone, even if we stop talking</li>
                        <li>I don't gossip or expose secrets</li>
                        <li>I don't switch sides when people argue</li>
                        <li>If you're in my life, I'll be loyal — quietly, not loudly</li>
                        <li>If you ever need me, I'll help without making a scene about it</li>
                        <li>But if I feel like I'm being taken for granted, I leave without noise</li>
                        <li>I stay for a long time if someone proves they are real</li>
                        <li>I don't replace people easily, but once I detach, it's permanent</li>
                    </ul>
                </div>
            </div>

            <div id="music" class="section">
                <span class="read-time">⏱ 1 min read</span>
                <div class="music-player">
                    <h2>My Vibe</h2>
                    <p>Music that feels real, not commercial nonsense. These playlists capture the energy I live by.</p>
                    
                    <div class="playlist-grid">
                        <div class="playlist-item" onclick="loadPlaylist('2poYJoFh5iBdLvYfptGJ5z', 0)">
                            <div class="playlist-name">🎵 Playlist 1</div>
                            <div style="color: #888; font-size: 0.9rem;">Tap to load</div>
                        </div>
                        <div class="playlist-item" onclick="loadPlaylist('4PWQV9dQpT7As9OTZBqrR8', 1)">
                            <div class="playlist-name">🎵 Playlist 2</div>
                            <div style="color: #888; font-size: 0.9rem;">Tap to load</div>
                        </div>
                        <div class="playlist-item" onclick="loadPlaylist('5DVUEqRL1EV8I9n65eBaAw', 2)">
                            <div class="playlist-name">🎵 Playlist 3</div>
                            <div style="color: #888; font-size: 0.9rem;">Tap to load</div>
                        </div>
                        <div class="playlist-item" onclick="loadPlaylist('7ABD15iASBIpPP5uJ5awvq', 3)">
                            <div class="playlist-name">🎵 Playlist 4</div>
                            <div style="color: #888; font-size: 0.9rem;">Tap to load</div>
                        </div>
                        <div class="playlist-item" onclick="loadPlaylist('6yYA6aUGp8qUTgQWWYkPkP', 4)">
                            <div class="playlist-name">🎵 Playlist 5</div>
                            <div style="color: #888; font-size: 0.9rem;">Tap to load</div>
                        </div>
                        <div class="playlist-item" onclick="loadPlaylist('37i9dQZF1DX7qK8ma5wgG1', 5)">
                            <div class="playlist-name">🎵 Playlist 6</div>
                            <div style="color: #888; font-size: 0.9rem;">Tap to load</div>
                        </div>
                    </div>

                    <div id="spotify-embed" class="playlist-embed"></div>
                    
                    <div style="margin-top: 20px; padding: 15px; background: rgba(239, 68, 68, 0.05); border-radius: 10px; border: 1px solid rgba(239, 68, 68, 0.2);">
                        <p style="font-size: 0.9rem; color: #888; margin: 0;">💡 If the player doesn't load, click the playlist title again or open it directly in Spotify.</p>
                    </div>
                </div>
            </div>

            <div id="evolution" class="section">
                <span class="read-time">⏱ 3 min read</span>
                <div class="card">
                    <h2>My Evolution</h2>
                    <div class="comparison">
                        <div class="before">
                            <h4>Before</h4>
                            <ul>
                                <li>I used to trust easily because I believed people meant what they said</li>
                                <li>I thought friendships lasted forever</li>
                                <li>I invested my time and energy into people without thinking twice</li>
                                <li>I apologized even when I wasn't wrong, just to keep peace</li>
                                <li>I cared too much and expected the same in return</li>
                                <li>I thought loyalty was normal</li>
                            </ul>
                        </div>
                        <div class="after">
                            <h4>Now</h4>
                            <ul>
                                <li>I don't trust anyone without proof</li>
                                <li>I don't believe in "forever" anymore</li>
                                <li>I don't waste energy on people who don't deserve it</li>
                                <li>I don't explain myself</li>
                                <li>If someone leaves, I won't chase</li>
                                <li>My peace is more important than keeping connections alive</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="card">
                    <h2>Why I Distance Myself</h2>
                    <ul>
                        <li>Because getting close always ends with disappointment</li>
                        <li>People change when they find someone better</li>
                        <li>I hate feeling like a burden, so I disappear before anyone gets tired of me</li>
                        <li>It's easier to stay silent than explain emotions to someone who won't care</li>
                        <li>Trust gets broken too easily and too often</li>
                        <li>Every time I opened up, it eventually got used against me</li>
                        <li>Loneliness hurts, but betrayal hurts more</li>
                        <li>Being alone feels safer</li>
                    </ul>
                </div>
            </div>

            <div id="truth" class="section">
                <span class="read-time">⏱ 1 min read</span>
                <div class="card">
                    <h2>Signs I'm Done With Someone</h2>
                    <ul>
                        <li>I stop messaging first</li>
                        <li>My replies become blunt, short, or delayed</li>
                        <li>I stop sharing anything personal</li>
                        <li>I don't argue — I just stop caring</li>
                        <li>Their presence doesn't affect my mood anymore</li>
                        <li>I don't ask what's wrong or try to fix things</li>
                        <li>I don't feel anger, just emptiness</li>
                        <li>If they walk away, I don't stop them</li>
                        <li>Once I mentally detach, the relationship is finished permanently</li>
                    </ul>
                </div>
            </div>

            <div id="interests" class="section">
                <span class="read-time">⏱ 2 min read</span>
                <div class="card">
                    <h2>What Fascinates Me</h2>
                    <p>I'm drawn to the darker corners of human nature — not because I glorify it, but because I want to understand what most people look away from.</p>
                    <ul>
                        <li>Crime, serial killers, psychological thrillers</li>
                        <li>Minds that run darker and deeper than normal</li>
                        <li>Stories with twisted logic, smart villains, and unpredictable endings</li>
                        <li>The psychology behind why people break, betray, or lose themselves</li>
                    </ul>
                </div>

                <div class="card">
                    <h2>Where I Find Peace</h2>
                    <p>While others chase the light, I find comfort in the darkness. Not the kind that scares — the kind that lets me breathe.</p>
                    <ul>
                        <li>Storms, thunder, lightning — the raw power of nature unleashed</li>
                        <li>Cold nights when the world goes quiet</li>
                        <li>Empty streets at 3 AM with nobody around</li>
                        <li>Moonlight filtering through clouds</li>
                        <li>Stars scattered across a black sky</li>
                        <li>Darkness that feels peaceful, not threatening</li>
                    </ul>
                </div>

                <div class="card">
                    <h2>What Speaks to Me</h2>
                    <ul>
                        <li>Music that hits like truth, not manufactured noise for the masses</li>
                        <li>Songs that put feelings into words when I can't</li>
                        <li>Lyrics that don't sugarcoat pain or pretend everything's fine</li>
                        <li>Writing thoughts people are too scared to say out loud</li>
                        <li>Art that makes you feel something real, even if it's uncomfortable</li>
                    </ul>
                </div>
            </div>

            <div id="likes" class="section">
                <span class="read-time">⏱ 2 min read</span>
                <div class="card">
                    <h2>What I Like</h2>
                    <ul>
                        <li>Music that feels real, not commercial nonsense</li>
                        <li>Deep conversations with someone who actually listens instead of pretending</li>
                        <li>Peace, silence, and late-night thoughts</li>
                        <li>Learning things alone because depending on others always ends badly</li>
                        <li>People who don't judge instantly and take time to understand</li>
                        <li>Honest answers, even if they are harsh</li>
                    </ul>
                </div>

                <div class="card">
                    <h2>What I Hate</h2>
                    <ul>
                        <li>Fake people who talk sweet but act different</li>
                        <li>Lies, even small ones — because small lies become big ones</li>
                        <li>Friends who are only available when they are bored</li>
                        <li>Being compared to others like I'm not enough</li>
                        <li>Promise makers who never keep a single one</li>
                        <li>People who pretend to care publicly but don't show effort privately</li>
                    </ul>
                </div>
            </div>

            <div id="boundaries" class="section">
                <span class="read-time">⏱ 3 min read</span>
                <div class="card warning">
                    <h2>Things I'll Never Tolerate</h2>
                    <ul>
                        <li>Fake loyalty — don't act loyal to my face and switch behind my back</li>
                        <li>Emotional manipulation or guilt-tripping</li>
                        <li>Disappearing when I need someone and then showing up like nothing happened</li>
                        <li>Using me only when it's convenient</li>
                        <li>Making fun of my feelings, trauma, or mental health</li>
                        <li>Betrayal even once — trust doesn't restart</li>
                        <li>Cheating, lying, or hiding things to avoid responsibility</li>
                        <li>Drama, attention-seeking, playing victim</li>
                        <li>People who talk behind my back instead of to my face</li>
                    </ul>
                </div>

                <div class="card">
                    <h2>If You Want To Be In My Circle</h2>
                    <h3>Rules & Boundaries</h3>
                    <ul>
                        <li>Don't lie. I don't forgive lies</li>
                        <li>Be consistent — don't talk a lot one day and vanish the next</li>
                        <li>Respect my silence. Sometimes I need space. Don't force me to talk</li>
                        <li>If you leave, stay gone. I don't allow repeated entrances</li>
                        <li>Keep private conversations private</li>
                        <li>Don't expect trust without earning it</li>
                        <li>Match energy — if I put effort, put effort back</li>
                        <li>Don't compare me to others</li>
                        <li>Don't play mind games</li>
                        <li>Loyalty matters more than sweet words or promises</li>
                        <li>If we have a problem, talk directly — no passive aggression</li>
                    </ul>
                </div>
            </div>

            <div id="friendship" class="section">
                <span class="read-time">⏱ 2 min read</span>
                <div class="card">
                    <h2>What I Look For In Real Friendship / Relationship</h2>
                    <ul>
                        <li>Loyalty without excuses</li>
                        <li>Consistency — don't treat me like an option</li>
                        <li>Effort — not words</li>
                        <li>Someone who doesn't disappear when they're bored</li>
                        <li>Someone who understands I'm quiet, not cold</li>
                        <li>Someone who stays through my silent days, not only my happy ones</li>
                        <li>Someone who respects boundaries</li>
                        <li>Someone who talks honestly, not manipulatively</li>
                        <li>Someone who values trust and doesn't play games</li>
                        <li>Someone who won't leave the moment things aren't perfect</li>
                    </ul>
                </div>

                <div class="card">
                    <h2>Final Word</h2>
                    <p>I don't need a lot of people in my life. I need real ones. I don't need constant attention. I need consistent loyalty. I don't need perfect people. I need honest ones.</p>
                    <p>If you can't match that energy, that's fine. Just don't waste my time pretending you can.</p>
                </div>
            </div>
        </div>
    </div>

    <footer>
        <div class="contact-section">
            <h3 style="color: var(--accent); font-size: 1.8rem; margin-bottom: 20px; letter-spacing: 0.1em;">CONNECT</h3>
            <a href="https://t.me/FadedWords17" target="_blank" class="telegram-link">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor" style="margin-right: 10px;">
                    <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.562 8.161c-.18 1.897-.962 6.502-1.359 8.627-.168.9-.5 1.201-.82 1.23-.697.064-1.226-.461-1.901-.903-1.056-.693-1.653-1.124-2.678-1.8-1.185-.781-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.139-5.062 3.345-.479.329-.913.489-1.302.481-.428-.008-1.252-.241-1.865-.44-.752-.244-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.831-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635.099-.002.321.023.465.141.121.099.155.232.171.326.016.095.036.312.02.482z"/>
                </svg>
                @FadedWords17
            </a>
        </div>
        <p style="margin-top: 40px;">No fake energy. No pretending. Just real.</p>
        <p>VARSHAL</p>
    </footer>

    <script>
        // Particles System
        const canvas = document.getElementById('particles-canvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const particles = [];
        const particleCount = 80;
        let mouse = { x: null, y: null, radius: 150 };

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2 + 0.5;
                this.speedX = Math.random() * 0.5 - 0.25;
                this.speedY = Math.random() * 0.5 - 0.25;
                this.opacity = Math.random() * 0.5 + 0.2;
            }

            update() {
                this.x += this.speedX;
                this.y += this.speedY;

                if (this.x > canvas.width) this.x = 0;
                if (this.x < 0) this.x = canvas.width;
                if (this.y > canvas.height) this.y = 0;
                if (this.y < 0) this.y = canvas.height;

                // Mouse interaction
                if (mouse.x && mouse.y) {
                    const dx = mouse.x - this.x;
                    const dy = mouse.y - this.y;
                    const distance = Math.sqrt(dx * dx + dy * dy);
                    
                    if (distance < mouse.radius) {
                        const force = (mouse.radius - distance) / mouse.radius;
                        const angle = Math.atan2(dy, dx);
                        this.x -= Math.cos(angle) * force * 3;
                        this.y -= Math.sin(angle) * force * 3;
                    }
                }
            }

            draw() {
                ctx.fillStyle = `rgba(239, 68, 68, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function init() {
            for (let i = 0; i < particleCount; i++) {
                particles.push(new Particle());
            }
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            
            for (let i = 0; i < particles.length; i++) {
                particles[i].update();
                particles[i].draw();
                
                // Connect nearby particles
                for (let j = i; j < particles.length; j++) {
                    const dx = particles[i].x - particles[j].x;
                    const dy = particles[i].y - particles[j].y;
                    const distance = Math.sqrt(dx * dx + dy * dy);
                    
                    if (distance < 120) {
                        ctx.strokeStyle = `rgba(239, 68, 68, ${0.1 * (1 - distance / 120)})`;
                        ctx.lineWidth = 0.5;
                        ctx.beginPath();
                        ctx.moveTo(particles[i].x, particles[i].y);
                        ctx.lineTo(particles[j].x, particles[j].y);
                        ctx.stroke();
                    }
                }
            }
            requestAnimationFrame(animate);
        }

        window.addEventListener('mousemove', (e) => {
            mouse.x = e.x;
            mouse.y = e.y;
        });

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });

        init();
        animate();

        // Custom cursor
        const cursorFollower = document.querySelector('.cursor-follower');
        const cursorDot = document.querySelector('.cursor-dot');
        
        let mouseX = 0, mouseY = 0;
        let followerX = 0, followerY = 0;
        
        document.addEventListener('mousemove', (e) => {
            mouseX = e.clientX;
            mouseY = e.clientY;
            
            cursorDot.style.left = mouseX + 'px';
            cursorDot.style.top = mouseY + 'px';
        });
        
        function animateCursor() {
            followerX += (mouseX - followerX) * 0.1;
            followerY += (mouseY - followerY) * 0.1;
            
            cursorFollower.style.left = followerX + 'px';
            cursorFollower.style.top = followerY + 'px';
            
            requestAnimationFrame(animateCursor);
        }
        animateCursor();

        // Parallax effect
        const parallaxText = document.querySelector('.parallax-text');
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            parallaxText.style.transform = `translate(-50%, calc(-50% + ${scrolled * 0.3}px))`;
        });

        // Quote rotator
        const quotes = [
            "Every saint has a past, and every sinner has a future. The line between them is thinner than you think.",
            "The most dangerous masks are the ones people wear when they smile.",
            "Society fears the monster, but creates the conditions that birth it.",
            "Normal is an illusion. What's normal for the spider is chaos for the fly.",
            "The darkest minds often understand the human condition better than the brightest.",
            "We're all capable of darkness. Some just have better control over their demons.",
            "The difference between genius and madness is measured only by success.",
            "Everyone is a moon, and has a dark side which they never show to anybody.",
            "In the depth of winter, I finally learned that within me there lay an invincible summer.",
            "Monsters are real. They're people, and they're the most terrifying thing of all.",
            "The mind is its own place, and can make a heaven of hell, a hell of heaven.",
            "We all go a little mad sometimes. Haven't you?",
            "The devil doesn't come dressed in a red cape. He comes as everything you've ever wished for.",
            "Behind every beautiful thing, there's been some kind of pain.",
            "The world breaks everyone, and afterward, some are strong at the broken places.",
            "I think the truly natural things are dreams, which nature cannot touch with decay.",
            "There is no exquisite beauty without some strangeness in the proportion.",
            "The oldest and strongest emotion of mankind is fear, and the oldest and strongest kind of fear is fear of the unknown.",
            "We make up horrors to help us cope with the real ones.",
            "Hell is empty and all the devils are here."
        ];

        let currentQuoteIndex = 0;

        function rotateQuote() {
            const quoteElement = document.getElementById('quote-text');
            quoteElement.style.opacity = '0';
            
            setTimeout(() => {
                currentQuoteIndex = (currentQuoteIndex + 1) % quotes.length;
                quoteElement.textContent = quotes[currentQuoteIndex];
                quoteElement.style.opacity = '1';
            }, 500);
        }

        // Initialize first quote
        document.getElementById('quote-text').textContent = quotes[0];
        
        // Rotate quotes every 8 seconds
        setInterval(rotateQuote, 8000);

        // Theme toggle
        function toggleTheme() {
            document.body.classList.toggle('blue-theme');
            const icon = document.getElementById('theme-icon');
            const text = document.getElementById('theme-text');
            
            if (document.body.classList.contains('blue-theme')) {
                icon.textContent = '☀️';
                text.textContent = 'Dark Mode';
            } else {
                icon.textContent = '🌙';
                text.textContent = 'Blue Mode';
            }
        }

        // Spotify player
        let currentPlaylist = null;

        function loadPlaylist(playlistId, index, event) {
            // Prevent default link behavior
            if (event) {
                event.preventDefault();
            }

            const embedContainer = document.getElementById('spotify-embed');
            const allItems = document.querySelectorAll('.playlist-item');
            
            // Remove active class from all
            allItems.forEach(item => item.style.borderColor = 'var(--border)');
            
            // Add active to clicked
            allItems[index].style.borderColor = 'var(--accent)';
            
            if (currentPlaylist === playlistId) {
                embedContainer.classList.remove('active');
                embedContainer.innerHTML = '';
                currentPlaylist = null;
                allItems[index].style.borderColor = 'var(--border)';
            } else {
                // Try to load embed with full height
                try {
                    embedContainer.innerHTML = `
                        <div style="position: relative; padding-top: 20px;">
                            <iframe 
                                style="border-radius:12px; width: 100%; height: 600px; border: none;" 
                                src="https://open.spotify.com/embed/playlist/${playlistId}?utm_source=generator" 
                                allowfullscreen="" 
                                allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" 
                                loading="eager">
                            </iframe>
                            <p style="text-align: center; margin-top: 15px; color: #666; font-size: 0.85rem;">
                                💡 Scroll inside the player to see all tracks. Not loading? <a href="https://open.spotify.com/playlist/${playlistId}" target="_blank" style="color: var(--accent);">Open directly in Spotify →</a>
                            </p>
                        </div>
                    `;
                    embedContainer.classList.add('active');
                    currentPlaylist = playlistId;
                    
                    // Scroll to player
                    setTimeout(() => {
                        embedContainer.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                    }, 300);
                } catch (error) {
                    console.error('Failed to load playlist:', error);
                    embedContainer.innerHTML = `
                        <div style="text-align: center; padding: 40px; background: rgba(239, 68, 68, 0.1); border-radius: 15px; border: 1px solid rgba(239, 68, 68, 0.3);">
                            <p style="color: var(--accent); font-size: 1.2rem; margin-bottom: 10px;">⚠️ Embed blocked</p>
                            <p style="color: #888;">
                                <a href="https://open.spotify.com/playlist/${playlistId}" target="_blank" style="color: var(--accent); text-decoration: underline;">
                                    Click here to open in Spotify
                                </a>
                            </p>
                        </div>
                    `;
                    embedContainer.classList.add('active');
                }
            }
        }

        // Section switching
        function showSection(sectionId, buttonElement) {
            const sections = document.querySelectorAll('.section');
            const buttons = document.querySelectorAll('nav button');
            const contentWrapper = document.querySelector('.content-wrapper');
            
            // Fade out
            contentWrapper.style.opacity = '0';
            
            setTimeout(() => {
                sections.forEach(section => {
                    section.classList.remove('active');
                });
                
                buttons.forEach(button => {
                    button.classList.remove('active');
                });
                
                document.getElementById(sectionId).classList.add('active');
                
                // Find and activate the clicked button
                buttons.forEach(button => {
                    if (button.textContent.toLowerCase().includes(sectionId) || 
                        button.getAttribute('onclick').includes(sectionId)) {
                        button.classList.add('active');
                    }
                });
                
                // Fade in
                contentWrapper.style.opacity = '1';
                
                // Smooth scroll to content
                window.scrollTo({
                    top: document.querySelector('.content-wrapper').offsetTop - 80,
                    behavior: 'smooth'
                });
            }, 300);
        }

        // Interactive hover effects for cards
        const cards = document.querySelectorAll('.card');
        cards.forEach(card => {
            card.addEventListener('mousemove', (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                
                const rotateX = (y - centerY) / 20;
                const rotateY = (centerX - x) / 20;
                
                card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-5px)`;
            });
            
            card.addEventListener('mouseleave', () => {
                card.style.transform = '';
            });
        });

        // Scroll reveal for cards
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, { threshold: 0.1 });

        cards.forEach(card => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(50px)';
            card.style.transition = 'all 0.8s cubic-bezier(0.4, 0, 0.2, 1)';
            observer.observe(card);
        });

        // Make content wrapper visible after load
        setTimeout(() => {
            document.querySelector('.content-wrapper').classList.add('visible');
        }, 100);

        // Loading Screen
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('loadingScreen').classList.add('hidden');
            }, 2000);
        });

        // Mouse Trail Effect
        const trails = document.querySelectorAll('.mouse-trail');
        let mouseTrailX = 0, mouseTrailY = 0;

        document.addEventListener('mousemove', (e) => {
            mouseTrailX = e.clientX;
            mouseTrailY = e.clientY;
        });

        function animateTrail() {
            let x = mouseTrailX;
            let y = mouseTrailY;

            trails.forEach((trail, index) => {
                trail.style.opacity = '0.6';
                trail.style.left = x - 5 + 'px';
                trail.style.top = y - 5 + 'px';
                
                const nextTrail = trails[index + 1] || trails[0];
                x += (parseFloat(nextTrail.style.left) || mouseTrailX) - x;
                y += (parseFloat(nextTrail.style.top) || mouseTrailY) - y;
            });

            requestAnimationFrame(animateTrail);
        }
        animateTrail();

        // Audio Ambiance Control
        const audioToggle = document.getElementById('audioToggle');
        const ambientAudio = document.getElementById('ambientAudio');
        let isPlaying = false;

        audioToggle.addEventListener('click', () => {
            if (isPlaying) {
                ambientAudio.pause();
                audioToggle.textContent = '🔇';
                audioToggle.classList.remove('playing');
                isPlaying = false;
            } else {
                ambientAudio.play();
                audioToggle.textContent = '🔊';
                audioToggle.classList.add('playing');
                isPlaying = true;
            }
        });

        // Set volume to 30%
        ambientAudio.volume = 0.3;
    </script>
</body>
</html>
